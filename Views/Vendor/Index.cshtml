@model IEnumerable<ECommWeb.Models.Vendor>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@* old create *@

<body style="background-color:#adb5bd;">


<h1>Vendor</h1>

<p>
  @*   <a asp-action="Create">Create New</a> *@
</p>
<table class="table" style="border:2px solid black">
    <thead>
        <tr>
            <th>
               Company Name:
            </th>
            <th>
              Address
            </th>
            <th>
              Email
            </th>       
            <th>
                @Html.DisplayNameFor(model => model.Phone)
            </th>
                <th>
                    GSTIN
                </th>
                <th>
                @Html.DisplayNameFor(model => model.Status)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CompanyName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AddressID)
            </td>
          
            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Phone)
            </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.GSTINID)
                    </td>
            <td>
                @Html.DisplayFor(modelItem => item.Status)
            </td>
            <td>
               @*  <a asp-action="Edit" asp-route-id="@item.Vendor_ID">Edit</a> | *@
                        <a asp-action="Details" class="btn btn-dark" asp-route-id="@item.VendorID">Details</a> 
               @*  <a asp-action="Delete" asp-route-id="@item.Vendor_ID">Delete</a> *@
            </td>
        </tr>
}
    </tbody>
</table>

</body>